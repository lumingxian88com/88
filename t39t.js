// 1. 這是您以後手動更新的地方 (電池核心)
var d26d = [
  ["2026/01/06", "01", "02", "06", "11", "33"],
  ["2026/01/05", "10", "16", "18", "34", "39"]
];

// 2. 這是 2025 的鐵帳底數 (固定不跑)
var base2025 = [31, 37, 37, 48, 42, 40, 56, 45, 41, 39, 44, 47, 38, 49, 32, 30, 27, 37, 41, 36, 39, 43, 39, 53, 36, 48, 54, 46, 43, 35, 44, 34, 35, 34, 40, 38, 37, 46, 39];

// 3. 自動計算並「塞進」 HTML 的功能
function autoUpdate() {
    var counts = [...base2025];
    d26d.forEach(row => {
        for (var i = 1; i <= 5; i++) {
            var n = parseInt(row[i]);
            if (n >= 1 && n <= 39) counts[n-1]++;
        }
    });

    // 這裡是關鍵：把結果組成原本 times 的樣子
    window.times = [d26d[0], ...counts];

    // 執行「塞」的動作：找 888888 這個位置
    var target = document.getElementById('888888');
    if (target) {
        target.innerHTML = "<b>📅 日期：</b>" + d26d[0][0] + " | <b>🔢 號碼：</b>" + d26d[0].slice(1).join(", ");
        target.style.color = "blue"; 
    }
}

// 網頁一開好就執行
window.addEventListener('load', autoUpdate);
